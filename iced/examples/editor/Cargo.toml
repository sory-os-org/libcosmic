[package]
name = "editor"
version = "0.1.0"
authors = ["Héctor Ramón Jiménez <hector@hecrj.dev>"]
edition = "2024"
publish = false

[dependencies]
iced = { workspace = true, features = ["highlighter", "tokio", "debug", "winit", "tiny-skia"] }

tokio = { workspace = true, features = ["fs"] }

rfd = { workspace = true }
